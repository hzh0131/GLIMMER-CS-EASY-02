# C-EASY-2各部分问题的回答
## Step-1
### 指针与结构体
1. 
- 
定义指针变量的方法一般有以下几种
```c
   int *p;   // 定义一个指向int类型的指针变量p
   char *chp;// 定义一个指向char的指针
   double *dp;//定义一个指向double的指针
```   
- 
同一个编译平台下，指针的大小是固定的（eg:64位系统中指针占8字节）。

- 
指针大小主要由编译平台决定

2. 
- [1]输出结果：20（x） 10(y)
- [2]原因：
- (1) `int x=10; int *p=&x; *p=20;``p`保存了 x 的地址，`*p`代表了这个地址所对应的数（即x本身），修改`*p=20`，所以**x=20**；
- (2)int arr[3]={3,6,9}; int *q = arr;`
    数组名 arr 代表首元素地址，`q = &arr[0]`，此时：
   `*arr` = arr[0] = 3；`*q`=arr[0]=3。
- (3)`y = ++*arr + *++q;`


*注意：`++*arr`等价于`++(*arr)`，`*++q`等价于`*(++q)`*

- `++*arr`：先解引用`*arr`得到 arr [0]=3，`++*arr`就是arr [0] 变成**4**，表达式值为 4
- `++q`：q 指针向后移动一位，指向 arr [1]（值 6）；`*++q`取值得到**6**
- 相加：`4 + 6 = 10` → y=10  
*最终 printf 打印 `x=20，y=10`*

3. 
- 野指针就是指向一块不受控制、无效内存地址的指针。危害：对野指针解引用 `*p`，程序崩溃。还可能破坏堆内存，引发内存损坏，程序随机闪退。
- 如何避免：
- [1]定义指针时初始化，不用就赋值 `NULL`
- [2]保证指针不越界，数组指针严格控制访问范围
- [3]函数返回值不要返回局部变量的地址

4. 
``` c
# include <stdio.h>
void swap(int *a,int *b)
{
    int temp;
    temp=*a;
    *a=*b;
    *b=*temp;


}
int main(void)
{
    int a,b;
    swap(&a,&b);    
    return 0;
}
```
### 什么是结构体
1. 
```c
#include <stdio.h>

struct PerInfo
   {
       char name[10];
       char sex;
       int age;
       double height;
   };
typedef struct PerInfo information;
int main()
{
   information p;
   p.age=18;
   return 0;
}
```
2. 结构体指针，就是一个专门用来存放结构体变量的起始内存地址的变量.eg:
   >information*ptr就是专门用来存放ptr结构体首元素地址的指针
    - *注：若要通过结构体指针来访问结构体中的变量有两种方式：*（eg）
    - 【1】ptr->age
    - 【2】(*ptr).age
3. 结构体的内存对齐规则就是编译器自动在成员之间 / 末尾填充一些空白字节，让每个成员的起始地址满足对齐要求（**该成员的起始偏移量，必须是它  的所占内存大小的整数倍。且结构体整体大小，必须等于最大成员所占内存大小的整数倍**），从而提升硬件读取效率。
4. 
```c
#include <stdio.h>
struct PerInfo
   {
       char name[10];//占用0~9字节
       char sex;//偏移量为10，10是1的整数倍，不需要补空白字节
       int age;//偏移量为11，不是4的倍数，需要补1个空白字节，从12开始占字节
       double height;//偏移量为16,是8的倍数,不需要补空白字节
   };
typedef struct PerInfo information;
int main()
{
   information p;
   int a=sizeof(p);
   printf("%d",a);
   return 0;
}
```
*最终计算结果为24(是8的整数倍)>10+1+4+8=23(因为中间补了一个空白字节)*
如果调整一下定义顺序,结构体大小可能会改变：
```c
struct Test1
{
    char c;
    int i;
    double d;
};
// 大小：16

struct Test2
{
    char c;
    double d;
    int i;
};
// 大小：24
```
## Step-2
### 什么是链表
1. 
- 数组是连续内存而链表是分散内存，靠指针串联。
- 数组的内存地址是连续的。所有元素紧紧挨在一起，知道首地址，就能通过下标直接算出任意元素地址。
- 而链表的内存是不连续的。每个节点单独分配内存，节点之间靠指针记录下一个节点的位置。
- 
|对比项|数组|链表|
|:---|:---|:---|
|内存分布|连续|离散|
|随机访问|支持随机访问，下标直接访问|不支持随机访问，只能从头遍历|
|插入 / 删除（中间）|开销大，需要移动后面大量元素|开销小，只需要修改指针指向|
|内存开销|只存数据本身|除数据外，额外存储指针，有内存开销|

2. 单向链表（单链表）的每一个节点由两部分组成：
- **数据域**：存放节点的数据
-  **指针域**：存放**下一个节点的地址**
> 特点：只能从头部往尾部单向遍历；不能反向访问前一个节点；最后一个节点的指针为`NULL`，表示链表结束。

### 实现基本的链操作
``` c
#include <stdio.h>
#include <stdlib.h>

// 定义单向链表节点
typedef struct Node {
    int data;           
    struct Node *next;  
} Node;

int main(void) {
    //创建唯一节点，data=0，next置NULL
    Node *head = (Node *)malloc(sizeof(Node));
    head->data = 0;
    head->next = NULL;

    // 验证链表
    Node *p = head;
    printf("链表节点数据：%d\n", p->data);

    // 释放内存
    free(head);
    head = NULL;
    return 0;
}
```
### 添加元素：
1. 设计函数：
``` c
typedef struct Node {
    int data;           
    struct Node *next;  
} Node;
Node *get_number(int val)
{
    Node *a = (Node *)malloc(sizeof(Node));
    if (a == NULL)
    {
        return NULL; 
    }
    a->data=val;
    a->next=NULL;
    return a;
}
```
2. 头插：
``` c
typedef struct Node {
    int data;           
    struct Node *next;  
} Node;
Node *toucha(int val,Node* head)
{
    Node* s=(Node*)malloc(sizeof(struct Node));
    if(s == NULL){
        return NULL;
    }
    s->data = val;
    s->next = head;
    head = s;
    return head;
}
```
3. 尾插
``` c
typedef struct Node {
    int data;           
    struct Node *next;  
} Node;

Node *weicha(int val,Node* head)
{
    Node* s=(Node*)malloc(sizeof(struct Node));
    Node *p = head;
    if(s== NULL)
    {return head;}
    if(head == NULL)
    {
        head = s;
        return head;
    }
    while(p->next != NULL)
    {
        p = p->next;
    }
    p->next = s;

    return head;

}
```
### 查找元素
```c
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef struct Node {
    int data;               
    struct Node *next;      
} Node;
//打印链表
void printList(Node *head) {
    // 使用临时指针p遍历，head保持不变
    Node *p = head;
    printf("链表内容：");
    while (p != NULL) {
        printf("%d ", p->data);
        p = p->next;
    }
    printf("\n");
}
```

``` c
bool findNode(Node *head, int n) {
    Node *p = head;
    int count = 1;  
    while (p != NULL) {
        if (p->data == n) {
            printf("%d",count);
            return true; // 找到第一个，直接返回
        }
        p = p->next;
        count++;
    }
    // 循环结束没找到
    return false;
}
```

### 删除与更改
```c
bool deleteNode(int n, Node* head)
{
    if (n < 1 || head == NULL)
        return false;
    if (n == 1)
        return false;

    Node* p = head;
    int cnt = 1;
    while (p != NULL && cnt < n - 1)
    {
        p = p->next;
        cnt++;
    }
    if (p == NULL || p->next == NULL)
        return false;

    Node* del = p->next;
    p->next = del->next;
    free(del);
    return true;
}
```
### 反转函数
``` c
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
typedef struct Node
{
    int data;
    struct Node*next;

}Node;

void localreverse(Node*head)
{
   if(head==NULL||head->next==NULL)
   {return ;}
   Node*beg=head->next;
   Node*end=head->next->next;
   while(end!=NULL)
   {
     beg->next=end->next;
     end->next=head->next;
     head->next=end;
     end=beg->next;


   }
   return;
}
```
